# Debug Journal

Complete one entry per bug. All six entries are required for full marks.

---

## Bug 1 — Silent RLS Block

| Field          | Your Entry |
| -------------- | ---------- |
| **Symptom** | The dashboard page loads successfully, but the shipments table displays "No Data Found". However, the `shipments` table in Supabase contains 5 rows confirmed through the Table Editor. No errors appear in the browser console, indicating that the query executes but returns no visible rows. |
| **Hypothesis** | **Observations:**<br>- The dashboard loads successfully but the shipments table displays "No Data Found".<br>- The Supabase Table Editor confirms that the `shipments` table contains 5 rows.<br>- No errors appear in the browser console.<br>- The query appears to execute successfully but returns an empty dataset.<br><br>**Possible hypotheses:**<br>1. Row Level Security (RLS) may be blocking SELECT access for the `anon` role used by the frontend client.<br>2. The Supabase query may be executing correctly but returning no rows due to missing SELECT policies.<br>3. The frontend may not have permission to read rows even though the table exists and contains data.<br><br>**Investigation:**<br>Inspection of the Supabase table configuration revealed that Row Level Security was enabled for the `shipments` table, but only an UPDATE policy existed. Because RLS denies access by default when no policy explicitly allows it, the frontend client using the `anon` role could not perform SELECT operations. As a result, the query returned an empty dataset without producing an explicit error. |
| **AI Prompt** | I'm debugging a Next.js logistics dashboard that fetches data from a Supabase table called `shipments`. <br><br> The dashboard loads but shows "No Data Found" even though the database contains 5 rows. <br><br> Observed behavior: <br> - The query does not produce an error but results in an empty dataset. <br> - No error appears in the console. <br> - Row Level Security is enabled on the table. <br><br> Could RLS cause a query to silently return empty results instead of throwing an error? <br> How can I inspect the current RLS policies to confirm whether access is being blocked? |
| **Fix** | Updated the Supabase database configuration to allow read access for the frontend client.<br><br>The following change was applied in the Supabase SQL Editor by creating a SELECT policy for the `shipments` table:<br>1. Added a new RLS policy that allows the `anon` role to perform SELECT operations on the `shipments` table.<br>2. Ensured the policy only grants read access without modifying existing UPDATE permissions.<br>3. Verified that the existing UPDATE policy remained unchanged to preserve intended security rules.<br><br>**Verification:**<br>- Reloading the dashboard now correctly displays all 5 shipment rows.<br>- The query returns actual data instead of an empty dataset.<br>- No database security rules were bypassed, and RLS remains enabled. |

---

## Bug 2 — Ghost Mutation

| Field          | Your Entry |
| -------------- | ---------- |
| **Symptom** | Changing the shipment status from the dropdown triggers a success notification ("Status updated successfully") with a check icon in the UI. However, after refreshing the page, the status reverts to its original value.<br><br>This behavior occurs for every status transition (e.g., Pending → In Transit or In Transit → Delivered). The UI appears to indicate success, but the change never persists in the database after refreshing the dashboard. |
| **Hypothesis** | **Observations:**<br>- The UI consistently shows a success notification after attempting to update the shipment status.<br>- Refreshing the dashboard immediately restores the original status.<br>- No errors appear in the browser console.<br>- Checking the Supabase dashboard confirms the `shipments` table rows never actually change.<br><br>**Possible hypotheses:**<br>1. UPDATE operations might be blocked by Row-Level Security (similar to Bug 1).<br>2. The mutation query might not correctly target the intended row.<br>3. The Supabase mutation may not be awaited, causing the server action to return before the update completes.<br>4. The mutation result may not be validated, causing the function to return success even when the update fails.<br><br>**Investigation:**<br>After inspecting `src/actions/update-status.ts`, I found that the Supabase mutation was executed without `await`, and the returned result was never inspected for errors. The function always returned `{ success: true }`, meaning the UI displayed success regardless of whether the database update actually succeeded. |
| **AI Prompt**  | I am debugging a Next.js Server Action that updates a shipment status in a Supabase table.<br><br>Observed behavior:<br>- Changing the shipment status from the dropdown shows a success notification.<br>- Refreshing the dashboard restores the original status.<br>- The Supabase `shipments` table does not reflect any changes after the update.<br>- No errors appear in the browser console.<br><br>The server action currently executes this mutation:<br>`supabase.from('shipments').update({ status }).eq('id', id)`<br><br>The function then immediately returns `{ success: true }`.<br><br>Could this behavior occur if the Supabase mutation is not awaited or if the mutation result is not checked for errors? |
| **Fix** | Updated the server action responsible for mutating shipment status in `src/actions/update-status.ts`.<br><br>The following minimal changes were applied:<br>1. Added `await` to the Supabase update query so the mutation completes before returning a response.<br>2. Extracted the `{ error }` object from the Supabase response to properly detect failed mutations.<br>3. Added an explicit error check that throws an exception when the database update fails instead of always returning success.<br><br>**Verification:**<br>- Changing shipment status now persists after refreshing the page.<br>- All valid status transitions correctly update the database.<br>- Failed mutations now surface errors instead of silently returning success. |

---

## Bug 3 — Infinite Loop

| Field          | Your Entry |
| -------------- | ---------- |
| **Symptom** | When opening `/dashboard`, the page freezes immediately and becomes unresponsive. The backend server logs show hundreds of repeated `GET /dashboard` requests every second even though the user is not interacting with the page. |
| **Hypothesis** | **Observations:**<br>- The issue occurs immediately when the dashboard loads.<br>- Server logs show continuous `GET /dashboard` requests.<br>- Requests occur without any user interaction.<br>- The DataTable component contains a `useEffect` that updates the URL using `router.push()` when sorting changes.<br><br>**Possible hypotheses:**<br>1. The `useEffect` dependency array may contain a value that changes on every render.<br>2. The effect may be triggering `router.push()` repeatedly, causing continuous navigation.<br>3. The navigation may cause the page to re-render, which triggers the effect again and creates a render loop.<br><br>**Investigation:**<br>Inspection of `src/components/data-table.tsx` revealed that the `useEffect` responsible for syncing sorting state to the URL query parameters was running repeatedly. The dependency array contained an unstable reference that changed during every render. Because the effect executed `router.push()`, each render triggered navigation to `/dashboard`, which caused the page to reload and the effect to run again, resulting in an infinite navigation loop. |
| **AI Prompt** | I am debugging a Next.js 15 application using the App Router and TanStack Table.<br><br>Observed behavior:<br>- Opening `/dashboard` causes the page to freeze.<br>- The server logs show hundreds of repeated `GET /dashboard` requests.<br>- The requests occur even when the user does nothing.<br>- No errors appear in the browser console.<br><br>In `src/components/data-table.tsx`, a `useEffect` updates the URL query parameters whenever the sorting state changes.<br><br>Possible hypotheses I am considering:<br>- The dependency used inside the `useEffect` might produce a new reference on every render.<br>- The effect might be triggering `router.push()` repeatedly, causing continuous navigation.<br>- The navigation might cause the page to re-render, which triggers the effect again and creates a render loop.<br><br>Could one of these explain the repeated `GET /dashboard` requests, and what would be the correct way to stabilize the dependency so the effect only runs when the sorting state actually changes? |
| **Fix** | Updated the sorting synchronization logic in the DataTable client component located in `src/components/data-table.tsx`.<br><br>The following minimal changes were applied:<br>1. Restricted the `useEffect` dependency array to `[sorting]` so the effect only runs when the sorting state changes.<br>2. Prevented the router navigation from executing on every render cycle.<br>3. Added an ESLint suppression comment to intentionally control the dependency behavior without introducing unstable references.<br><br>**Verification:**<br>- The dashboard loads normally without freezing.<br>- Network traffic shows only a single request instead of hundreds of repeated requests.<br>- Sorting interactions correctly update the URL parameters without causing infinite navigation loops. |

---

## Bug 4 — The Invisible Cargo

| Field          | Your Entry |
| -------------- | ---------- |
| **Symptom** | The dashboard loads successfully and displays all shipment rows, but the Cargo column appears incomplete for most entries. Instead of showing both the cargo item and its weight, the UI only displays the weight unit (`kg`) without the item name. The database contains valid cargo records, indicating that cargo data exists but is not rendered correctly in the table. |
| **Hypothesis** | **Observations:**<br>- The Cargo column displays only the weight unit (`kg`) for several shipments.<br>- Some shipments correctly display a dash (`—`) when no cargo exists.<br>- The database contains valid cargo information for affected rows.<br>- No runtime errors appear in the browser console.<br><br>**Possible hypotheses:**<br>1. The cargo data returned by Supabase may have a different structure than the `Shipment` TypeScript type.<br>2. The Cargo column renderer may be accessing fields that do not exist in the returned object.<br>3. The relationship query from Supabase may be returning `cargo_details` as an array instead of a single object.<br><br>**Investigation:**<br>Adding temporary console logging in the Cargo column revealed that `cargo_details` was returned as an array (e.g., `[ { item: "Laptop Batch A", weight_kg: 120 } ]`). However, the `Shipment` type and UI renderer expected `cargo_details` to be a single object. Because the UI accessed `cargo.item`, but the actual value was an array, the item name appeared as `undefined`, causing the column to display only the weight unit. |
| **AI Prompt** | I am debugging a Next.js 15 application using the App Router and TanStack Table.<br><br>Observed behavior:<br>- The Cargo column shows only `kg` instead of displaying both the item name and weight.<br>- Some rows display `—`, which correctly represents shipments without cargo.<br>- The database contains valid cargo records.<br>- Console logs reveal that `cargo_details` is returned as an array such as `[ { item: "Laptop Batch A", weight_kg: 120 } ]`.<br><br>The TypeScript type for shipments defines `cargo_details` as an object:<br>`{ item: string; weight_kg: number } \| null`.<br><br>Could this mismatch between the Supabase response shape (array) and the expected TypeScript type (object) cause the UI to render incomplete cargo information? What would be the correct way to normalize the data before passing it to the table component? |
| **Fix** | Updated the cargo data handling logic in `src/app/dashboard/page.tsx` to normalize the structure returned from the database.<br><br>The following minimal changes were applied:<br>1. Introduced a `normalizeCargoDetails` helper function that safely converts the `cargo_details` array returned by Supabase into a single object expected by the UI.<br>2. Applied the normalization step when mapping shipment records before passing them to the `DataTable` component.<br>3. Ensured that null cargo values remain unchanged so rows without cargo still render the correct placeholder dash.<br><br>**Verification:**<br>- Cargo item names and weights now display correctly in the table.<br>- Rows without cargo continue to display the dash placeholder as expected.<br>- The UI now consistently renders cargo data without requiring structural changes to the database. |

---

## Bug 5 — The Unreliable Search

| Field          | Your Entry |
| -------------- | ---------- |
| **Symptom** | When typing quickly in the search input, the table sometimes displays results that do not match the latest query. For example, typing "Medical" may briefly show unrelated shipments or all rows instead of only the expected results. The issue becomes more noticeable when the network is slow, where typing quickly causes inconsistent search results even though the final input value is correct. |
| **Hypothesis** | **Observations:**<br>- Each keystroke in the search input triggers a new request to the `searchShipments` server action.<br>- Multiple search requests are sent before previous ones finish.<br>- Under simulated slow network conditions using Chrome DevTools (Slow 3G), responses return at different times.<br>- The UI updates the table whenever a request finishes.<br><br>**Possible hypotheses:**<br>1. Multiple asynchronous search requests are being triggered without cancelling or ignoring previous ones.<br>2. Responses may return out of order, allowing older results to overwrite newer ones.<br>3. The UI does not verify that the response corresponds to the most recent search input.<br><br>**Investigation:**<br>Inspection of `src/components/data-table.tsx` showed that the `handleSearch` function sends a request to `searchShipments(query)` on every input change and immediately updates the table with `setTableData(results)`. Because the requests run asynchronously, responses may resolve in a different order than they were sent. Without any mechanism to track the latest request, slower responses from earlier queries can overwrite results from newer queries, producing incorrect search results. |
| **AI Prompt** | In `src/components/data-table.tsx`, the search input triggers the `searchShipments` server action through the `handleSearch` function whenever the input value changes.<br><br>Observed behavior:<br>- Each keystroke in the search input sends a new request to the `searchShipments` server action.<br>- Typing quickly (e.g., "medical") generates multiple requests in rapid succession.<br>- Under simulated slow network conditions (Chrome DevTools Slow 3G), responses often return in a different order than the requests were sent.<br>- The UI immediately updates the table with `setTableData(results)` whenever a response resolves.<br>- As a result, a slower response from an earlier query can overwrite the results of a later query, causing the table to temporarily display data that does not match the current input value.<br><br>Given that the application uses a server action pattern and asynchronous requests cannot be avoided, what would be the correct way to ensure that only the most recent search request is allowed to update the table state while preserving the existing architecture? |
| **Fix** | Updated the search handling logic in the DataTable component located in `src/components/data-table.tsx` to prevent race conditions during rapid typing.<br><br>The following minimal changes were applied:<br>1. Introduced a `requestIdRef` using `useRef` to track the most recent search request.<br>2. Incremented the request identifier each time a new search query is triggered.<br>3. Updated the response handler to only apply results when the returned request ID matches the latest active request.<br><br>**Verification:**<br>- Typing quickly no longer causes outdated results to overwrite newer ones.<br>- Search results consistently match the most recent query input.<br>- The solution preserves the existing server action architecture without modifying backend logic. |

---

## Bug 6 — The Persistent Ghost

| Field          | Your Entry |
| -------------- | ---------- |
| **Symptom** | When attempting to change a shipment status from **Delivered** back to **Pending**, the UI initially shows a success notification, but after refreshing the page the shipment status remains **Delivered**. Other status transitions such as **Pending → In Transit** and **In Transit → Delivered** work correctly and persist after refresh. |
| **Hypothesis** | **Observations:**<br>- The status update is triggered from the `handleStatusUpdate` function in `src/components/columns.tsx`.<br>- The server action `updateShipmentStatus` sends an update query to the Supabase `shipments` table.<br>- Attempting the transition **Delivered → Pending** produces a database error message: `"Cannot revert a delivered shipment to pending status"`.<br>- A trigger named `check_status_transition` exists in `legacy_setup.sql` that calls `prevent_delivered_to_pending()`.<br>- The trigger explicitly raises an exception if `OLD.status = 'Delivered'` and `NEW.status = 'Pending'`.<br><br>**Possible hypotheses:**<br>1. The database trigger is intentionally blocking the Delivered → Pending transition as a business rule.<br>2. The server action is throwing an error when the database rejects the update.<br>3. The UI does not properly catch errors from the server action, resulting in misleading behavior.<br><br>**Investigation:**<br>Inspection of `legacy_setup.sql` confirmed that the database trigger raises an exception when attempting to revert a delivered shipment back to pending. The server action correctly receives this error, but the client component originally did not wrap the call to `updateShipmentStatus` in a `try/catch`. This caused the exception to propagate to the runtime instead of being presented as a user-facing error message. |
| **AI Prompt** | I am debugging a Next.js App Router application where shipment statuses are updated using a Supabase server action. The UI allows selecting a new shipment status from a dropdown menu in `src/components/columns.tsx`.<br><br>Observed behavior:<br>- Most status transitions such as **Pending → In Transit** and **In Transit → Delivered** update successfully and persist after refresh.<br>- Changing a shipment from **Delivered → Pending** fails and the server returns the error `"Cannot revert a delivered shipment to pending status"`.<br>- The database schema contains a trigger `check_status_transition` defined in `legacy_setup.sql` that raises an exception when attempting this transition.<br>- The server action receives this error, but the client component currently does not catch exceptions thrown by the action.<br>- Instead, the error propagates to the runtime and appears as a failed request in the browser console.<br><br>Given that the database trigger enforces an intentional business rule and must not be removed, what would be the correct way to handle this situation in the client component so that the user receives a clear error notification while preserving the existing database constraint and server action architecture? |
| **Fix** | Updated the client-side status update handler in `src/components/columns.tsx` to properly handle errors thrown by the server action.<br><br>The following minimal changes were applied:<br>1. Wrapped the `updateShipmentStatus` server action call inside a `try/catch` block to capture exceptions raised by the database trigger.<br>2. Preserved the existing success flow by keeping the `toast.success('Status updated successfully')` notification when the mutation succeeds.<br>3. Added a `toast.error(message)` call in the catch block to display a clear user-facing error message when the database rejects the status transition.<br><br>**Verification:**<br>- Valid status transitions continue to update successfully.<br>- Attempting the invalid transition **Delivered → Pending** now displays an error notification instead of a false success message.<br>- The database trigger remains intact and continues to enforce the business rule. |
